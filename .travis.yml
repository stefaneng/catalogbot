language: python
python:
  - 2.7

install:
  - pip install .
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - coverage run --source=catalogbot setup.py test
  - coverage report -m

after_script:
  - coveralls --verbose